<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jing.admin.mapper.WorkflowMapper">
    <!-- 工作流DTO结果映射 -->
    <resultMap id="WorkflowDTOMap" type="com.jing.admin.model.dto.WorkflowDTO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="json_data" property="jsonData"/>
        <result column="version" property="version"/>
        <result column="status" property="status"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_user_name" property="createUserName"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="update_user_name" property="updateUserName"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 分页查询工作流（关联用户表） -->
    <select id="selectWorkflowPageWithUser" resultMap="WorkflowDTOMap">
        SELECT
        w.id,
        w.name,
        w.description,
        w.json_data,
        w.version,
        w.status,
        w.create_user_id,
        cu.username AS create_user_name,
        w.update_user_id,
        uu.username AS update_user_name,
        w.create_time,
        w.update_time
        FROM
        tb_workflow w
        LEFT JOIN
        tb_users cu ON w.create_user_id = cu.uuid
        LEFT JOIN
        tb_users uu ON w.update_user_id = uu.uuid
        <where>
            <if test="query.name != null and query.name != ''">
                AND w.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="query.status != null and query.status != ''">
                AND w.status = #{status}
            </if>
            <if test="query.createUserId != null and query.createUserId != ''">
                AND w.create_user_id = #{createUserId}
            </if>
        </where>
        ORDER BY w.create_time DESC
    </select>

</mapper>